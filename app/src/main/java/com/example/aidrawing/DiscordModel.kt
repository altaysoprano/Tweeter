package com.example.aidrawing

import android.os.Build
import android.util.Log
import androidx.annotation.RequiresApi
import com.example.aidrawing.common.Constants.TOKEN

class DiscordModel() {

/*
    init {
        val bot = DiscordApiBuilder().setToken(TOKEN).login().isDone
        Log.d("Mesaj", bot.toString())
    }
*/

/*
    @RequiresApi(Build.VERSION_CODES.N)
    fun sendMessage(channelId: String, message: String) {
        val channel = bot.getTextChannelById(channelId).get()
        channel.sendMessage(message)
    }
*/


}
